A monospace, tracked, uppercase label — used for categories, metadata, and section eyebrows.

```jsx
<Tag>Selected works</Tag>
<Tag tone="blue" outline>Branding</Tag>
<Tag tone="muted">2024</Tag>
```

Props: `tone` (`ink` `blue` `green` `muted`), `outline` (1px box vs bare text).
