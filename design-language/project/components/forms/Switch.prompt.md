A flat, square-cornered toggle — track fills hyperlink blue when on, no shadow, no bounce.

```jsx
<Switch checked={dark} onChange={setDark} label="Reduce motion" />
```

Props: `checked`, `onChange(next)`, `label`, `disabled`.
