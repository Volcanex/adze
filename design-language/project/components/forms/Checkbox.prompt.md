A square checkbox (the brand never rounds) with a blue fill when checked and a serif label.

```jsx
<Checkbox checked={ok} onChange={setOk} label="Keep me posted" />
```

Props: `checked`, `onChange(next)`, `label`, `disabled`.
