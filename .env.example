# Copy this to .env and fill in your values
# .env is gitignored — never commit it

# Admin token for the super-admin fallback (set a strong random string)
DEV_ADMIN_TOKEN=change-me-to-something-strong

# Email used for SSL certificate registration (Let's Encrypt)
CERTBOT_EMAIL=your@email.com
